import 'package:flutter/material.dart';

class Contact {
  String name;
  String phone;
  String date;
  Color color;
  String fileName;

  Contact(this.name, this.phone, this.date, this.color, this.fileName);
}
