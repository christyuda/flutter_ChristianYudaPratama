class Contact {
  final String name;
  final String phone;
  // final String date;
  // final String color;
  // final String filename;
  // Contact(this.name, this.phone, this.date, this.color, this.filename);
  Contact(
    this.name,
    this.phone,
  );
}
